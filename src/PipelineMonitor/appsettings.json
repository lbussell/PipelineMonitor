{
  "NLog": {
    "variables": {
      "logDirectory": "${specialfolder:folder=LocalApplicationData}/PipelineMonitor/${environment:DOTNET_ENVIRONMENT:whenEmpty=Production}/logs"
    },
    "targets": {
      "logfile": {
        "type": "File",
        "fileName": "${var:logDirectory}/run-${date:format=yyyy-MM-dd-HHmmss}.log"
      }
    },
    "rules": [
      {
        "logger": "Microsoft.*",
        "minLevel": "Debug",
        "writeTo": "logfile"
      },
      {
        "logger": "PipelineMonitor.*",
        "minLevel": "Trace",
        "writeTo": "logfile"
      }
    ]
  }
}
